Remuco - Linux remote media player control

Rhythmbox Adapter
===============================================================================

Installation and usage ........................................................

To install, run:

$ make install

Alternatively copy the directory 'remythm' manually to the Rhythmbox plugin
directory (usually ~/.gnome2/rhythmbox/plugins/).

On next startup of Rhythmbox, the plugin should be active already.

You can configure Remuco with the file ~/.config/remuco/rhythmbox/conf (this
file will be created automatically with default values once Rhythmbox has been
started the first time after installing the Remuco plugin). 

To uninstall, run:

$ make uninstall

Alternatively delete the directory 'remythm' from the Rhythmbox plugin
directory.

===============================================================================

More information: visit http://remuco.sf.net